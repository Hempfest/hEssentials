package org.spigotmc.hessentials.commands.announcements;

import java.util.Random;

public class Announcements {
	
	
	
	public static void runAnnounce() {
		Random r = new Random();
		int amount = r.nextInt(20);
		if (amount == 1) {
			
			return;
		} else if (amount == 2) {
			
			return;
		} else if (amount == 3) {
			
			return;
		} else if (amount == 4) {
			
			return;
		} else if (amount == 5) {
			
			return;
		}
		
	}
	
}
